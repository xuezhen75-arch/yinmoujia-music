# 音谋加 H5 服务端 - 部署手册

## 一、本地运行（开发测试）

### 1. 安装 Node.js
确保已安装 Node.js 18+：
```bash
node --version
```

### 2. 安装依赖
```bash
cd 音谋加H5服务端
npm install
```

### 3. 配置 TTAPI Key（必须）

TTAPI 是第三方 Suno API 服务平台，支持国内访问：

1. **注册账号**：https://www.suno-api.io
2. **获取 Key**：控制台 → API密钥 → 创建新密钥
3. **设置环境变量**：

```bash
# Windows
set TT_API_KEY=你的TTAPI密钥

# Linux/Mac
export TT_API_KEY=你的TTAPI密钥
```

> 如果你有其他 Suno API 服务商的 Key，也可以设置 `SUNO_API_KEY` 环境变量，代码会自适应。

### 4. 启动服务
```bash
npm start
```

访问 http://localhost:3000 即可看到页面。

---

## 二、阿里云部署（正式环境）

### 1. 购买服务器
- 推荐：阿里云轻量应用服务器
- 配置：2核2G / 50GB SSD / 100Mbps
- 系统：Ubuntu 22.04 LTS
- 费用：约 ¥30/月

### 2. 连接服务器
```bash
ssh root@你的服务器IP
```

### 3. 安装 Node.js
```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 验证
node --version
npm --version
```

### 4. 上传代码
```bash
# 在本地项目目录压缩
cd 音谋加H5服务端
zip -r ../yinmoujia.zip *

# 上传到服务器（需要先安装 lrzsz）
# 在服务器执行：
apt install -y lrzsz

# 然后在本地执行上传：
sz yinmoujia.zip

# 或使用 scp：
scp yinmoujia.zip root@你的服务器IP:/root/
```

### 5. 解压并安装
```bash
cd /root
unzip yinmoujia.zip
cd 音谋加H5服务端
npm install --production
```

### 6. 配置环境变量
```bash
# 设置 TTAPI Key（必需）
echo 'TT_API_KEY=你的TTAPI密钥' > .env

# 或者设置 SUNO_API_KEY
echo 'SUNO_API_KEY=你的密钥' >> .env
```

### 7. 安装 PM2（进程管理器）
```bash
npm install -g pm2
```

### 8. 启动服务
```bash
pm2 start server.js --name yinmoujia

# 设置开机自启
pm2 startup
pm2 save
```

### 9. 配置 Nginx（反向代理 + HTTPS）
```bash
apt install -y nginx
```

创建配置文件：
```bash
nano /etc/nginx/sites-available/yinmoujia
```

写入以下内容（替换 YOUR_DOMAIN 为你的域名）：
```nginx
server {
    listen 80;
    server_name YOUR_DOMAIN;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

启用配置：
```bash
ln -s /etc/nginx/sites-available/yinmoujia /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### 10. 配置 SSL（可选但强烈推荐）
使用 Let's Encrypt 免费证书：
```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d YOUR_DOMAIN
```

---

## 三、验证部署

访问 http://你的服务器IP 或 https://你的域名

测试功能：
1. 选择心情、场景、风格
2. 点击生成
3. 查看结果页面

---

## 四、日常维护

### 查看日志
```bash
pm2 logs yinmoujia
```

### 重启服务
```bash
pm2 restart yinmoujia
```

### 查看状态
```bash
pm2 status
```

### 更新代码
```bash
# 上传新代码后
pm2 restart yinmoujia
```

---

## 五、故障排查

### 端口被占用
```bash
# 查找占用端口的进程
lsof -i:3000

# 杀死进程
kill -9 PID
```

### 数据库问题
```bash
# 删除数据库重新初始化
rm yinmoujia.db
pm2 restart yinmoujia
```

### 内存不足
```bash
# 查看内存
free -h

# 增加 swap
dd if=/dev/zero of=/swapfile bs=1M count=1024
mkswap /swapfile
swapon /swapfile
```

---

## 六、费用汇总

| 项目 | 费用 |
|------|------|
| 阿里云服务器 | ¥30/月 |
| 域名 | ¥30/年 |
| SSL证书 | 免费（Let's Encrypt）|
| **合计** | **约 ¥360/年** |
