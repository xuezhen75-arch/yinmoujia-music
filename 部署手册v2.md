# 音谋加 - 部署手册 v2.0

## 快速启动（本地）

```bash
cd 音谋加H5服务端
npm install
npm start
```

**带API Key启动：**
```bash
AI_MUSIC_API_KEY=sk-your-key ADMIN_TOKEN=你的管理口令 node server-v2.js
```

访问：
- 主页面：`http://localhost:3000/`
- 管理后台：`http://localhost:3000/admin.html`

---

## 部署方案

### 方案一：Vercel（推荐 · 免费）

1. **上传代码到 GitHub**
   - 创建新仓库，上传本目录所有文件
   - 在 Vercel 导入仓库

2. **配置环境变量**
   - `AI_MUSIC_API_KEY` = 你的 suno-api.io Key
   - `ADMIN_TOKEN` = 管理后台口令（自定义）

3. **访问**
   - Vercel 会分配一个 `xxx.vercel.app` 域名
   - 直接分享这个链接给用户即可

**注意：** Vercel 默认会缓存 Node.js 服务，第一次冷启动约 10 秒

---

### 方案二：Railway（推荐 · $5免费额度/月）

1. 注册 https://railway.app
2. New Project → Deploy from GitHub
3. 选择仓库
4. 添加环境变量：
   - `AI_MUSIC_API_KEY`
   - `ADMIN_TOKEN`
5. Railway 自动识别 Node.js 并启动

---

### 方案三：腾讯云轻量应用服务器（国内推荐）

1. 购买轻量服务器（2核2G，约 60元/月）
2. 安装 Node.js 18+：
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. 上传代码：
   ```bash
   # 在本地打包
   zip -r yinmoujia.zip . -x "node_modules/*"

   # 上传到服务器后解压
   unzip yinmoujia.zip
   cd 音谋加H5服务端
   npm install --production
   ```

4. 启动：
   ```bash
   AI_MUSIC_API_KEY=sk-xxx ADMIN_TOKEN=你的口令 node server-v2.js &
   ```

5. 域名解析：添加 DNS A 记录指向服务器 IP

---

### 方案四：Cloudflare Workers（边缘部署 · 免费）

适合海外用户，Workers 不支持 Node.js 完整特性，建议用 Vercel

---

### 方案五：Docker 部署

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production
COPY . .
EXPOSE 3000
CMD ["node", "server-v2.js"]
```

```bash
docker build -t yinmoujia .
docker run -d -p 3000:3000 \
  -e AI_MUSIC_API_KEY=sk-xxx \
  -e ADMIN_TOKEN=口令 \
  yinmoujia
```

---

## 管理后台使用

1. 打开 `你的域名/admin.html`
2. 输入 `ADMIN_TOKEN`（启动时设置的环境变量）
3. 点击「生成」批量创建解锁码
4. 将解锁码发给转账用户

---

## 功能说明

| 功能 | 说明 |
|------|------|
| 免费次数 | 每设备 3 次免费 |
| 解锁码 | 1 元/个，有效期 1 年 |
| 管理员 | 生成解锁码、查看统计数据 |
| 数据存储 | JSON 文件，无需数据库 |

---

## 常见问题

**Q: 音乐生成需要多久？**
A: 通常 60~120 秒，请耐心等待

**Q: 如何查看 API Key 余额？**
A: 访问 https://www.suno-api.io 登录查看

**Q: 解锁码格式是什么？**
A: `YINMJ-XXXX-XXXX`，由管理后台自动生成

**Q: 如何修改价格？**
A: 目前的逻辑是固定 1 个解锁码 = 1 元，如需调整请联系开发者
